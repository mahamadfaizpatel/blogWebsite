<%- include("partials/header") %>

<h1>Home</h1>
<p><%= startingContent %></p>

<% posts.forEach(function(post){ %>
    <h1><%= post.title %></h3>
    <p><%= post.body %></p>
<% }); %>

<%- include("partials/footer") %>